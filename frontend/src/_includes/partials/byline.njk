<p>by
  <span class="author">
    <a href="{{ 'People' | route(author | slugify) }}">{{ author }}</a>
  </span>
  on
  <time datetime="{{ date }}" class="datetime">{{ date | asPostDate }}</time>
</p>

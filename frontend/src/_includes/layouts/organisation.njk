---
layout: layouts/agent.njk
---

{% set funded = projects | fundedProjects(agentInstance) %}
{% if funded.length > 0 %}
  <div id="funded">
    <h2>Projects funded</h2>
    <ul>
      {% for project in funded %}
        <li>
          <a href="{% route project.slug, 'Projects' %}">{{ project.name }}</a>
        </li>
      {% endfor %}
    </ul>
  </div>
{% endif %}
